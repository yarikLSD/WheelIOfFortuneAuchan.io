  //Проверка цвета 1 ые 4 человека
{const btn = document.getElementById('click-to-hide1');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide2');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide3');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide4');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide5');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide6');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide7');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide8');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide9');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide10');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide11');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide12');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide13');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide14');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide15');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide16');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide17');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide18');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide19');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide20');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide21');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide22');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide23');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide24');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide25');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide26');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide27');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide28');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
{const btn = document.getElementById('click-to-hide29');

  let index = 0;
  
  const colors = ['red', 'green'];
  
  btn.addEventListener('click', function onClick() {
    btn.style.backgroundColor = colors[index];
    btn.style.color = 'white';
  
    index = index >= colors.length - 1 ? 0 : index + 1;
  });
}
//Проверка текста
{
  document.getElementById("click-to-hide1").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide2").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide3").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide4").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide5").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide6").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide7").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide8").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide9").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide10").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide11").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide12").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide13").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide14").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide15").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide16").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide17").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide18").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide19").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide20").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide21").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide22").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide23").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide24").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide25").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide26").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide27").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide28").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}
{
  document.getElementById("click-to-hide29").addEventListener(
    "click",
    function(event) {
      if (event.target.value === "Убрать") {
        event.target.value = "Добавить";
      } else {
        event.target.value = "Убрать";
      }
    },
    false
  );
}

